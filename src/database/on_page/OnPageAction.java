package database.on_page;

/**
 * Interface that implements the actions that can be performed on a page.
 */
public interface OnPageAction {
    /**
     * Method that executes the on page action.
     */
    void execute();
}
